server:
  port: 8082

spring:
  application:
    name: catalog-service
  data:
    mongodb:
      uri: mongodb+srv://kaushalyadilmiav2003_db_user:sHWqKCG0WzJ2plmG@cluster0.hqgdgvh.mongodb.net/bookloop_catalog_db?retryWrites=true&w=majority

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port:8082}

management:
  endpoints:
    web:
      exposure:
        include: "*"