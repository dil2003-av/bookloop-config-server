server:
  port: 8082

spring:
  application:
    name: catalog-service
  data:
    mongodb:
      uri: mongodb+srv://kaushalyadilmiav2003_db_user:Ijse%401234@bookloop-catlog.sxvqwla.mongodb.net/bookloop_db?retryWrites=true&w=majority&appName=bookloop-catlog

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port:8082}

management:
  endpoints:
    web:
      exposure:
        include: "*"