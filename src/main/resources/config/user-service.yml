server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://34.142.152.151:3306/bookloop_mysql?useSSL=false&allowPublicKeyRetrieval=true
    username: devuser
    password: App@1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port:8081}